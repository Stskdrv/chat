import AuthWrapper from "../../components/AuthWrapper";
import LoginForm from "../../components/LoginForm";


const Login = () => {
    return (
       <AuthWrapper FormComponrnt={LoginForm} />
    )
};

export default Login;
